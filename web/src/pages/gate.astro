---
import GatePage from './_shared/GatePage.astro';

/**
 * The app's deep-link handling opens https://manourying.manouri.ovh/gate?code=…
 * directly — CLAUDE.md is explicit this URL must never move. /fr/gate and
 * /ar/gate are additions people reach via the language switcher or their
 * browser's own language, not the app's hardcoded target.
 */
---

<GatePage locale="en" />
