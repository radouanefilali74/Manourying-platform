---
import InstallPage from './_shared/InstallPage.astro';

/**
 * The app's INSTALL_URL points at this exact, unprefixed URL — CLAUDE.md is
 * explicit it must never move. /fr/install and /ar/install are additions.
 */
---

<InstallPage locale="en" />
