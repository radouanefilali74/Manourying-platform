---
import WhatThisIsPage from './_shared/WhatThisIsPage.astro';

/**
 * The page the spec demands, at a stable URL that must never move.
 *
 * If this ever needs renaming, leave a redirect: the URL is printed in App
 * Store review notes and linked from inside the app, and a dead link here is
 * worse than an ugly one.
 *
 * The actual markup lives in `_shared/WhatThisIsPage.astro` — this file is
 * just the English route, unprefixed because English is the default locale.
 */
---

<WhatThisIsPage locale="en" />
